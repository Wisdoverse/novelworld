[advisories]
ignore = []

# ttf-parser (unmaintained) remains an informational warning because it is
# transitive through an already-latest upstream release. It moves to ignores
# only if it becomes release-blocking with an actionable fix.
